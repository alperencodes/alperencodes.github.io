# Legacy identity assets

Preserved from the previous site for reference, outside the public build.

- `AC.png`: previous AC logo, formerly `public/AC.png`.
- `favicon.ico`: previous favicon, formerly `src/app/favicon.ico`.

The new header uses the text wordmark `alperen.` and the site has a new SVG favicon.
The factual content of the removed Next.js implementation is recorded in
`CONTENT_REFERENCE.md`; Git history also retains the implementation.
